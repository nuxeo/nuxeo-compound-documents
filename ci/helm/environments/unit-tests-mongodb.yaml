# these values are required by helmfile-base.yaml.gotmpl releases
nuxeo:
  enabled: false
mongodb:
  enabled: true
postgresql:
  enabled: false
opensearch:
  enabled: true
kafka:
  enabled: true

# values definition
global:
  labels:
    dev.nuxeo.com/usage: utests
